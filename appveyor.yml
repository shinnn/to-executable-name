init:
  - git config --global core.autocrlf input

shallow_clone: true

version: '{build}'

skip_tags: true

environment:
  matrix:
    - nodejs_version: '1'

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install tape

build: off

test_script:
  - ps: 'node --harmony_arrow_functions test.js #PowerShell'
  - node --harmony_arrow_functions test.js

cache:
  - node_modules
